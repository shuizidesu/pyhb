# EGA-IHB

EGA-IHB provides generic harmonic-balance and arc-length continuation utilities for nonlinear dynamic response analysis.

The reusable library code lives in `ega_ihb`. System-specific models and runnable scripts live in `examples`.
